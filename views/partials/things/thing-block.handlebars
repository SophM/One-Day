{{!-- to list the things --}}
<li class="list-group-item">
	{{name}}
    {{!-- if the thing hasn't been done, create a button "Done!" --}}
    {{#if notDone}}
	    <button class="has-been-done-BTN" data-id="{{id}}">Done!</button>
    {{/if}}
</li>